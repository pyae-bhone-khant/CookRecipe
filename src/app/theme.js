"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFA245",
    },
    secondary: {
      main: "#dc004e",
    },
  },
});

export default theme;
